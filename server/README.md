# Server

## Purpose

To host a static website from `../client/build`.

## Usage

To run for dev:

```shell script
npm run serve
```

To build for prod:

```shell script
npm run build
```

Note: the build will output in `/dist`